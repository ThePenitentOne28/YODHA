name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl -X GET "https://buvmqkgurywfadapamjx.supabase.co/rest/v1/" \
          -H "apikey: sb_publishable_5iQt2xlb0DXb8pcuXEdDAg_4GQp-VYQ"